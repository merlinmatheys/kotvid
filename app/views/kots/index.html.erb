<table class="liste_kot">

	<tr>
		<td class="tdkot">Quartier</td>
		<td class="tdkot">Adresse</td>
		<td class="tdkot">Nombre de chambre dans le communautaire</td>
  	</tr>


  <% @kots.each do |kot| %>
  	<tr>
  		<td class="tdkot"><%= kot.addresse %></td>
    	<td class="tdkot"><%= link_to "Détails", kot_path(kot) %></td>
    	<td class="tdkot">Patrick</td>
  	</tr>
  <% end %>
</table>

<h2>Tu veux rendre service à la communauté ? <%= link_to "Ajoutes ton kot", new_kot_path %></h2>

<div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
