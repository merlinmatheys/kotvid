<h2><%= @kot.addresse %></h2>
<p><%= @kot.prix %>€</p>
<p><%= @kot.quartier %></p>
<p><%= link_to "Revenir à tous les kots", kots_path %></p>
<% if @kot.user_id == current_user.id %>
  <td class="tdkot"><%= link_to "Modifier", edit_kot_path(@kot) %></td>
  <td class="tdkot"><%= link_to 'Supprimer', kot_path(@kot), data: {:confirm => 'Etes-vous sûr ?'}, :method => :delete %></td>
<% end %>

<div class="kot_photos">
<% @kot.photos.each do |photo| %>
  <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
<% end %>
</div>
